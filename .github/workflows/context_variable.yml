name: Contect variable Demo

on:
    workflow_dispatch

env:
    place: "Inside the workflow"

jobs:
    demo_job:
        runs-on: ubuntu-latest
        steps:
            - name: Printing github replated context vaiables
              run: |
                echo "Repository: ${{ github.repository }}"
                echo "Event Name: ${{ github.event_name }}"
                echo "Actor: ${{ github.actor }}"
                echo "Workflow: ${{ github.workflow }}"
                echo "User: ${{ github.user }}"
                echo "Job container: ${{ job.container.image }}"
            